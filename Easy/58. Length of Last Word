public int lengthOfLastWord(String s) {
        int count = 0;
        char[] Alpha = s.toCharArray();
        int i = Alpha.length - 1;
        while(i >= 0 && Alpha[i] == ' ' ){
            i --;
        }
        while(i >= 0 && Alpha[i] != ' ' ){
                count ++;
                i--;
        }
        return count;
    }
